#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef pair<int, int> ii;
typedef vector<int> vi;
typedef vector<ii> vii;
#define INF 1000000000

int main() {
	int N, M;
	cin >> N >> M;
	vector<string> villain(N);
	for (int i=0; i<N; i++) cin >> villain[i];
	
	for (int i=0; i<N; i++) {
		
	}
	
	return 0;
}