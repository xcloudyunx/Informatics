#include <bits/stdc++.h>

using namespace std;

int main() {
	int N, A, B, C;
	cin >> N >> A >> B >> C;
	
	vector<int> nums;
	
	int total = N*A;
	total -= B;
	total -= 2*C;
	nums.push_back(B);
	nums.push_back(C);
	nums.push_back(C);
	if (C > B)
	
	for (int i=2; i<N; i++) {
		
	}
	
	return 0;
}