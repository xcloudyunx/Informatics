#include <bits/stdc++.h>

using namespace std;

int main() {
	int N, K;
	cin >> N >> K;
	
	vector<int> scores(N);
	
	int score = 0;
	
	for (int i=0; i<N; i++) {
		cin >> scores.at(i);
		score += scores.at(i);
	}
	
	priority_queue<int> 
	
	return 0;
}